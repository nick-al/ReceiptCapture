package com.applepie.receiptcapture;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class Mockup4 extends AppCompatActivity {
    //TextView mback;
   // Button mCategory;
   // @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.photo_list_activity);

        /*
        mback=(TextView)findViewById(R.id.back);
        mCategory=(Button) findViewById(R.id.category);
        mback.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent intent=new Intent(Mockup4.this,Mockup3.class);
                startActivity(intent);
                finish();
            }
        });

        mCategory.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent intent=new Intent(Mockup4.this,Mockup5_2.class);
                startActivity(intent);
                finish();
            }
        });*/
    }
}
